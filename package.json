{
  "name": "alex-antablin-portfolio",
  "version": "2.0.0",
  "private": true,
  "description": "Personal portfolio for Alexandra Blair Antablin — a static, no-build, ocean-themed site.",
  "scripts": {
    "start": "sirv . --port 4321 --dev --host",
    "dev": "sirv . --port 4321 --dev --host"
  },
  "devDependencies": {
    "sirv-cli": "^2.0.0"
  }
}
